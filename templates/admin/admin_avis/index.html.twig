{% extends 'base.html.twig' %}

{% block title %}Avis index{% endblock %}

{% block body %}
<section>
    <h1>Avis index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Message</th>
                <th>Note</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for avi in avis %}
            <tr>
                <td>{{ avi.id }}</td>
                <td>{{ avi.message }}</td>
                <td>{{ avi.note }}</td>
                <td>
                    {% include "admin/admin_avis/_delete_form.html.twig" %}
                    <a href="{{ path('admin.avis.edit', {'id': avi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin.avis.new') }}">Create new</a>
</section>
{% endblock %}
