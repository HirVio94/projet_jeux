{% extends 'base.html.twig' %}

{% block title %}Ajouter genre{% endblock %}

{% block body %}
<main>
<section>
    <h1>Ajouter un genre</h1>
    <a href="{{ path('admin.genre') }}" class="valider">Retour</a>
    {% if error %}
    <span>Genre déja enregistré</span>
    {% endif %}
    
    {{ include('admin/admin_genre/_form.html.twig') }}

    
</section>
</main>
{% endblock %}
