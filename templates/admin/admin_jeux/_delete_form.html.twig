<form method="post" action="{{ path('admin.jeux.delete', {'id': jeu.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce jeu');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jeu.id) }}">
    <button class="btn">Delete</button>
</form>
